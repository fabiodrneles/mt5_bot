[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mt5bot"
version = "1.8.1"
description = "Bot de trading automatizado para MetaTrader 5 — Fase 3: CI/CD GitHub Actions + Maestro Go resiliente"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Fabio Dornele"}
]

dependencies = [
    "MetaTrader5>=5.0.45",
    "pytz>=2023.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "coverage",
]

[project.scripts]
mt5bot = "main:main"

[tool.setuptools]
py-modules = ["main", "config", "indicators", "strategy", "executor", "logger", "persistence", "tui", "dashboard", "tracker", "risk_calculator"]